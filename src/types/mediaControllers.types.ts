export type SendMediaRequestQueryTypes = {
  privateChatId?: string;
  channelId?: string;
  groupId?: string;
};
export type UploadedFileTypes = {
  status: string;
  fileName: string;
  filePath: string;
  fileType: string;
};
